# Implementation Plans — frontend kit (`example-web` + related)

Generated by `/improve` (adversarial-trimmed) on **2026-07-31**.  
**Planned against:** `3ae7932` (+ uncommitted W0 fixes may already be in the worktree).

Execute **in order**. Do not start a plan until its dependencies are **DONE**.  
Each executor: read the full plan, honor STOP conditions, update this table.

## Execution order & status

| Plan | Wave | Title | Priority | Effort | Depends on | Status |
|------|------|-------|----------|--------|------------|--------|
| [001](001-ship-route-boot-vite-host.md) | **W0** | Ship routeTree id/path fix + Vite host bind | P0 | S | — | DONE |
| [002](002-fix-design-system-e2e-ba.md) | **W1** | Repair design-system e2e for Better Auth + admin path | P0 | S | 001 | DONE |
| [003](003-invite-and-query-error-ux.md) | **W2** | Invite return-URL + error UX (gate, admin lists, health) | P1 | M | 001 | DONE |
| [004](004-reset-token-and-feedback-page.md) | **W3** | Strip reset token from URL + feedback pathname-only | P1 | S | 001 | DONE |
| [005](005-auth-org-characterization-tests.md) | **W4** | AuthGate/org/schema tests + fail-closed `useOrgContext` | P1 | M | 003 | DONE |
| [006](006-kit-fe-polish-optional.md) | **W5** | Optional polish (AlertDialog, roles i18n, Empty, docs) | P2 | M | 002, 005 | DONE |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED (reason)` | `REJECTED (reason)`

## Dependency graph

```text
001 (route boot + vite host)
 ├── 002 (e2e smoke — needs working SPA + 127.0.0.1)
 ├── 003 (invite/error UX)
 │     └── 005 (tests assume gate/error UX patterns exist)
 └── 004 (security S — independent of 003)
006 (optional) ← after 002 + 005 green
```

- **001** unblocks browser work and e2e default URL.
- **002** may run in parallel with **003/004** after 001, but prefer 002 early for a machine proof.
- **005** after **003** so tests cover the new invite/retry behavior, not the broken spinner.
- **006** is stoppable: ship W0–W4 without it if time/risk is tight.

## Recommended PR slicing

| PR | Plans | Notes |
|----|-------|--------|
| PR-A | 001 | One-liner correctness; ship first |
| PR-B | 002 | Script + optional `docs/testing.md` path note |
| PR-C | 003 | Multi-tenant UX; human review if auth redirects |
| PR-D | 004 | Security hygiene; short review |
| PR-E | 005 | Tests + orgContext throw |
| PR-F | 006 | Optional kit polish (separate) |

Do **not** squash W0–W5 into one kitchen-sink PR.

## Findings considered and rejected (do not re-plan)

| Finding | Why rejected |
|---------|----------------|
| Split `app-shell.tsx` (~490 LOC) | Normal SaaS shell size; churn without tests first. Revisit only if gates tests force seams. |
| ~~Select/Empty dogfood~~ | **Done post-W5** — OrgPicker + invite role Select; notes/keys/design-system Empty. |
| ~~seed-local D1 re-migrate~~ | **Done** — skip SQL re-exec when `d1_migrations` present. |
| Route-level code splitting | Example SPA still small; no TTI evidence. |
| Drop/subpath recharts from `@gosilex/ui` | No bundle report; tree-shake likely OK. |
| Chart `dangerouslySetInnerHTML` sanitize | Zero example-web consumers. |
| Feedback SVG MIME allowlist as P0 | Depends on Spark render; backlog package later. |
| FAB `VITE_API_URL` absolute endpoint | Kit same-origin proxy; consumer split-host docs only. |
| Raise coverage floor % | Prefer named contracts; ratchet % after 005. |
| UI package EN “Close” labels as blocking | Package API design (DIR-02); not in W0–W4. |
| Playwright in CI | After local e2e green (002); B7 backlog. |
| Generic open `next` post-login for all routes | Open-redirect surface; W2 is **invite-only** allowlist. |

## Adversarial constraints (all executors)

1. **Open redirect:** any post-login `next` / return path MUST be relative and allowlisted. No absolute URLs, no `//`.
2. **Never commit secrets** or paste seed passwords into plans beyond what’s already public in local-only banners.
3. **Do not** edit `packages/ui` for product brand (zero-edit contract).
4. Unrelated worktree noise (`apps/example-api/scripts/seed-local.ts`) is **out of scope** unless a plan names it.
