> **Tracked as** [#75](https://github.com/Roxabi/roxabi-boilerplate-cf/issues/75) — do not re-file.

# Kit D1 human walk — 2026-08-13

Local `example-api` `:8787` + `example-web` `:5173` + Mailpit `:8025`.  
Screenshots: `/tmp/kit-validate-b/screenshots/` (not committed).  
`EMAIL_TRANSPORT` flipped `log` → `smtp` for Mailpit, then restored to `log`.

## Pass

| Check | Evidence |
|---|---|
| Login password `demo@` / `demo-b@` / `staff@` / `super@` / `team-reader@` | land `/admin` or `/app` as expected |
| Notes IDOR (`demo` vs `demo-b`) | each sees only own seed notes |
| Org switch staff Acme (admin) ↔ Beta (member) | « Membres » disappears on Beta |
| Super sees 5 orgs (catalogue); staff sees 2 memberships | `/admin/orgs` |
| Catalogue create as Acme admin | `WIDGET` created |
| Org IDOR | `team-reader` on Team Client does **not** see Acme `WIDGET` |
| Mint `sk_` with active org | 200 · `GET /api/me` `authMethod=api_key` org-bound Acme |
| MCP `smoke:stdio` | exit 0 (`ping` ok) |

## Fail / residual

| Sev | Finding |
|---|---|
| **P1** | `EMAIL_TRANSPORT=smtp` is **Node-only** — Worker throws. Mailpit documented in README **does not receive** Worker mail. Demo email + invite + magic-link all 500. |
| **P1** | Magic-link UI shows « Vérifiez votre e-mail » while `POST /api/auth/sign-in/magic-link` is **500**. Anti-enum copy hides the transport failure. Mailpit empty. |
| **P1** | `team-reader` (Lecteur) can **create** a catalogue item (`LEAK`). Server accepted write. Grants UI says reader = lecture on `demo`. |
| **P2** | `demo@` without org: mint key → 400 `organizationId is required`, toast « Données invalides ». Fail-closed OK; copy opaque. |
| **P2** | Invite row not created when email send fails (fail-closed). Correct, but no local invite path while smtp is unusable. |
| **P3** | `smoke:stdio` `whoami` → `status: bad_config` (no live `sk_` in MCP env). Expected unless key wired. |
| **P3** | Platform modules page is badges only for staff (no toggle). Super catalogue not click-tested. |

## Not claimed

Platform JTBD D2/D3, second compose, Circle-on-kernel.
