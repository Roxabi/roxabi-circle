# Quality gates — monorepo Chemin A (apps + packages).
# Edit stack.yml quality_gates if present; keep this in sync.
: "${QG_FILE_MAX:=300}"
: "${QG_FILE_ROOTS:=apps packages}"
: "${QG_FILE_EXEMPTIONS:=tools/file_exemptions.txt}"
: "${QG_FILE_METRIC:=raw}"
