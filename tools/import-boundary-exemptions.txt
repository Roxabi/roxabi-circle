# Exemptions — import-boundary gate (R1–R4).
# Format: <exact/relative/importer/path>  # <reason> — <issue>
# Path is the importer file relative to monorepo root (posix separators).
# Scope: FILE-WIDE — the entire importer is skipped (all edges in that file).
# Prefer fixing the edge over permanent exemptions; use sparingly.
# Active lines WITHOUT a non-empty reason after # fail the gate (exit 2).
#
# Example (commented):
# packages/auth/src/evil.ts  # temporary dual-boot — #0
