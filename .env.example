# Root placeholders for local tooling (no secrets).
# API Worker secrets live in apps/example-api/.dev.vars (see .dev.vars.example).
VITE_API_URL=
# Mirror FEEDBACK_ENABLED on the API when testing the Signaler button locally.
VITE_FEEDBACK_ENABLED=
