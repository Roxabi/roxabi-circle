/** Dogfood alias — tables live in `@kit/auth/schema` (ADR-0008 D6). */
export {
  baAccount,
  baInvitation,
  baMember,
  baOrganization,
  baSession,
  baUser,
  baVerification,
  betterAuthDrizzleSchema,
} from '@kit/auth/schema'
