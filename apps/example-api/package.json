{
  "name": "@kit/example-api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "db:migrate": "wrangler d1 migrations apply example-api-local --local",
    "db:seed": "bun scripts/seed-local.ts",
    "db:seed:reset": "bun scripts/seed-local.ts --reset",
    "db:reset": "bun run db:migrate && bun run db:seed:reset",
    "build": "wrangler deploy --dry-run --outdir=dist"
  },
  "dependencies": {
    "@kit/auth": "workspace:*",
    "@kit/core": "workspace:*",
    "@kit/db": "workspace:*",
    "@kit/email": "workspace:*",
    "@kit/storage": "workspace:*",
    "@kit/types": "workspace:*",
    "better-auth": "^1.6.23",
    "drizzle-orm": "^0.44.0",
    "hono": "^4.7.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.11.1",
    "typescript": "^5.9.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.20.0"
  }
}
